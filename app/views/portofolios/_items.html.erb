<p><%= link_to items.title , portofolio_show_path(items)%></p>
<p><%= items.sub_title%></p>
<p><%= items.body%></p>
<p><%= image_tag items.thumb_image unless items.thumb_image.nil?%></p>
<%= link_to 'edit portofolio', edit_portofolio_path(items) %>
<%= link_to 'Delete Porofolio', portofolio_path(items), method: :delete, data:{confirm: 'Are you sure?' }%>
